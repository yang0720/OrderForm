<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- 进度条样式-->
    <style name="utils_material_design_dialog" parent="Theme.AppCompat.Dialog">
        <item name="android:windowFrame">@null</item>  <!--边框-->
        <item name="android:windowNoTitle">true</item>  <!--无标题-->
        <item name="android:windowBackground">@color/transparent</item>  <!--背景透明-->
        <item name="android:windowIsFloating">true</item><!--是否浮现在activity之上-->
        <item name="android:windowIsTranslucent">true</item><!--半透明-->
        <item name="android:backgroundDimEnabled">true</item><!--模糊-->
        <item name="android:windowContentOverlay">@null</item>
    </style>

    <style name="utils_radius_dialog" parent="@android:style/Theme.Dialog">
        <item name="android:windowFrame">@null</item>
        <item name="android:windowIsFloating">true</item>
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:background">@android:color/transparent</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:backgroundDimEnabled">true</item>
        <item name="android:backgroundDimAmount">0.6</item>
    </style>

    <style name="btn_16_cf">
        <item name="android:background">@drawable/def_btn</item>
        <item name="android:textSize">@dimen/dd_16</item>
        <item name="android:textColor">@color/dc_f</item>
        <item name="android:ellipsize">end</item>
        <item name="android:lines">1</item>
        <item name="android:gravity">center</item>
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">match_parent</item>
    </style>

    <style name="txt_16_c3">
        <item name="android:textSize">@dimen/dd_16</item>
        <item name="android:textColor">@color/dc_3</item>
        <item name="android:ellipsize">end</item>
        <item name="android:lines">1</item>
        <item name="android:gravity">center</item>
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">match_parent</item>
    </style>

    <!-- Base application theme. -->
    <style name="BaseAppTheme" parent="Theme.AppCompat">
        <!-- Customize your theme here. -->
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:windowFullscreen">false</item>
        <!-- toolbar（actionbar）颜色 -->
        <item name="colorPrimary">@color/baseColorPrimaryDark</item>
        <!-- 状态栏颜色 -->
        <item name="colorPrimaryDark">@color/baseColorPrimaryDark</item>
        <!-- 窗口的背景颜色 -->
        <item name="android:windowBackground">@color/colorBackground</item>
        <!-- 控件被选中时显示的颜色 -->
        <item name="colorAccent">@color/baseColorAccent</item>
    </style>
</resources>
